export enum RoomStatus {
    PREPARE = 0,
    HAPPING = 1,
    FINISHED = 2,
    CANCEL = 3,
    PENDING = 4,
    PREPARE_FINISH = 5,
}

export enum RoomSetting {
    TIME_REPLY = 30,
    TIME_DISPLAY_TOAST = 5000,
}

export enum RoomType {
    KAHOOT = 0,
    HOMEWORK = 1
}